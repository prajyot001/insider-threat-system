function Login() {
  return (
    <div style={styles.container}>
      <h2>Sign In Page</h2>
    </div>
  );
}

const styles = {
  container: {
    height: "100vh",
    backgroundColor: "#000",
    color: "white",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
};

export default Login;